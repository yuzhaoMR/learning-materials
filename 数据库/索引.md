# 索引

> 数据库的索引实现和非主键的二级索引

从数据结构角度:
B-Tree 索引,数据结构就是一颗 B+树。
Hash 索引，Hash 索引比较的是进行 Hash 运算之后的 Hash 值，所以它只能用于等值的过滤，不能用于基于范围的过滤。基本不用！
R-Tree 索引，仅支持 geometry 数据类型，也基本不用！

至于非主键的二级索引，这个实际上问的就是非聚簇索引！非聚簇索引本身就是一颗 B+树，其根节点指向聚簇索引的 B+树
